name: "MoChilia's Action Demo"
description: 'Greet someone and record the time'

inputs:
  who-to-greet: 
    description: 'Who to greet'
    required: true
    default: 'world'

outputs:
  time: # id of output
    description: 'The time we greeted you'

branding:
  icon: 'box'
  color: 'blue'

runs:
  using: 'node16'
  main: 'dist/index.js'
